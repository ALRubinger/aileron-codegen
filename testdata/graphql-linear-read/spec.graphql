type Query {
  """Fetch one Linear issue by its unique identifier."""
  issue(
    """Unique identifier (UUID) of the Linear issue."""
    id: String!
  ): Issue
}

type Issue {
  id: ID!
  identifier: String!
  title: String!
  description: String
}
